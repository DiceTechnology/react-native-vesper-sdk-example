import { Environment } from "@dicetechnology/react-native-vesper-sdk";

export interface Config {
    USERNAME: string;
    PASSWORD: string;
    REALM: string;
    API_KEY: string;
    ENV: Environment;
}